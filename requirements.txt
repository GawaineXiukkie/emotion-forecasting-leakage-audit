# Mac-only, no GPU. Torch uses MPS or CPU automatically.
numpy>=1.24
torch>=2.1
scikit-learn>=1.3
matplotlib>=3.9
tqdm>=4.65
gdown>=5.0   # only needed to download the COSMIC feature zip, see README Data section
